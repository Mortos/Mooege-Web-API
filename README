THIS IS MY INITIAL WEB ACCOUNT INTEGRATION FOR MOOEGE EMULATOR CONSIDER THAT MY ENGLISH I VERY BAD.
i use this debugged function in mooege code to compare php output to mooege SRP, in the first steap i print in console
the salt so i can use it in php code by adding it manually and test the progress of protocol.

* Script is beta has to be rewritten and clean up, but seems to work
-IMPORTANT: has to be tested if bytes on db is retrived correctly by mooege emu!
TODO:
* cleanup code and rewrite it in readble style ( most important)
* support for mysql
* better grafic interface
* support for check that is used real email
* add change password options
* perform login and session for see account stats and progress.